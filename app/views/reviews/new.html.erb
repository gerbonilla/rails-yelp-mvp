<div class="page-header-yelp">
  <h2>Add a Review</h2>
</div>

<%= simple_form_for [@restaurant, @review] do |f| %>

<%= f.input :rating, collection: (0..5).to_a, prompt: "Choose" %>
<%= f.input :content %>
<%= f.submit "Add Review", class: "btn btn-gray yelp-btn" %>

<% end %>
